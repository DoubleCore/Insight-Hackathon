# 半导体产业链图谱系统交付说明

## 目录结构

- `apps/api-server`：后端 API 服务，包含问答、管理端、链路观测、决策资料、产业搜索等接口。
- `apps/admin-portal`：管理端前端，默认端口 `7871`。
- `apps/user-portal`：用户端前端，默认端口 `7870`。
- `data`：演示数据、证据数据、Graphiti 导入资料和决策信号资料。
- `docs`：答辩说明和开发计划文档。
- `scripts`：导入、校验、启动和数据处理脚本。
- `tests`：项目级数据处理测试。

## 不包含内容

压缩包不包含以下本地运行产物：

- `node_modules`
- `dist`
- `.pytest_cache`
- `__pycache__`
- `data/runtime`
- 本地密钥文件、`.env` 文件
- vendored 开源仓库目录，例如 `graphiti-main`、`LightRAG-main`

## 环境要求

- Python 环境：Python 3.11 及以上；本机可使用已有 `task1-semiconductor-ui` 环境。
- Node.js / pnpm：前端依赖通过各自目录下的 `pnpm-lock.yaml` 安装。
- Neo4j：默认连接 `bolt://127.0.0.1:7687`，用户名 `neo4j`，密码 `password`。
- 大模型与搜索服务密钥：需要自行配置 SiliconFlow、Bocha、Tavily 等密钥；交付包不包含密钥。

## 安装依赖

后端：

```powershell
cd apps\api-server
python -m pip install -e .
```

管理端：

```powershell
cd apps\admin-portal
pnpm install
```

用户端：

```powershell
cd apps\user-portal
pnpm install
```

## 本地启动

在项目根目录运行：

```powershell
.\start-acceptance-platform.bat
```

默认地址：

- 用户端：`http://127.0.0.1:7870`
- 管理端：`http://127.0.0.1:7871`
- API：`http://127.0.0.1:8001`
- Neo4j Browser：`http://127.0.0.1:7474`

停止服务：

```powershell
.\stop-acceptance-platform.bat
```

## 验证命令

后端测试：

```powershell
cd apps\api-server
python -m pytest tests -q
```

管理端：

```powershell
cd apps\admin-portal
npm test -- --run
npm run build
```

用户端：

```powershell
cd apps\user-portal
npm test -- --run
npm run build
```
